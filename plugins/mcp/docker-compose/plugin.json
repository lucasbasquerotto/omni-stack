{
  "id": "mcp-server-docker-compose",
  "name": "Docker Compose MCP Server",
  "version": "0.1.0",
  "description": "Docker compose tools \u2014 run compose commands (up, down, ps, logs, exec)",
  "type": "mcp",
  "entrypoint": {
    "command": "mcp-server-docker-compose",
    "args": [],
    "transport": "stdio"
  },
  "config_schema": [
    {
      "key": "WORKSPACE_DIR",
      "label": "Workspace Directory",
      "type": "string",
      "default": "/opt/workspace",
      "description": "Allowed workspace directory (for project_dir validation)"
    }
  ]
}