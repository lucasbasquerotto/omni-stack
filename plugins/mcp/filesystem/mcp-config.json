{
  "servers": [
    {
      "name": "filesystem",
      "transport": "stdio",
      "command": "/app/target/release/mcp-server-filesystem",
      "args": [],
      "timeout_secs": 60,
      "max_retries": 2,
      "allowed_tools": [
        "*"
      ],
      "env": {
        "OMNI_DATA_DIR": "${OMNI_DATA_DIR:-/opt/data}",
        "WORKSPACE_DIR": "${WORKSPACE_DIR:-/opt/workspace}",
        "RUST_LOG": "info"
      }
    }
  ]
}